version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5431:5431"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: diego123
      POSTGRES_DB: balance_sales_db